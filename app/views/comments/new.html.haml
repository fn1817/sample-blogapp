.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

    -# @comment.article = commentsテーブルのarticle_idを使って、対応するArticleオブジェクトを返す
    -# オブジェクトを渡しているのにidに展開されるのはRailsが自動でやってくれているから
    = form_with(model: @comment, url: article_comments_path(@comment.article), local: true) do |f|
      %div
      = f.label :content, '内容'
      %div
      = f.text_area :content
      = f.submit '保存', class: 'btn-primary'
